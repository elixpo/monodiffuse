# Core (Phase 0 baseline + Phase 1)
torch>=2.1
torchvision>=0.16
numpy
scipy
matplotlib
tqdm

# Phase 1 (MonoDiffuse) extras
lpips        # perceptual reconstruction loss for the binary VAE
einops       # tensor reshaping in the binary U-Net / latent grid
