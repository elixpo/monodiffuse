# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Environments
.venv/
venv/
env/
.python-version

# Datasets (downloaded at runtime: MNIST, CIFAR-10, ...)
data/
*.gz

# Runtime outputs (keep curated grids in results/ explicitly with `git add -f`)
results/generated*.png
samples/
runs/
wandb/
*.log

# Notebooks / editors
.ipynb_checkpoints/
.vscode/
.idea/
.DS_Store

# Large checkpoints written during training (the published baseline weights in
# checkpoints/*.pth are tracked explicitly; new ad-hoc runs are not)
checkpoints/*AAA*.pth
checkpoints/*.ckpt
